package com.carddemo.parityprobe;

import com.carddemo.support.PostgresIntegrationTest;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.jdbc.core.JdbcTemplate;

import static org.junit.jupiter.api.Assertions.assertEquals;

/** Temporary parity probe (NOT committed): second subclass of PostgresIntegrationTest. */
class ProbeSecondSubclassIT extends PostgresIntegrationTest {
    @Autowired JdbcTemplate jdbc;
    @Test void queriesUsers() { assertEquals(Integer.valueOf(10), jdbc.queryForObject("select count(*) from users", Integer.class)); }
}
