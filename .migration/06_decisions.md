# 06 — Decision log (dated, APPEND-ONLY)

Every decision that a later session must not re-derive: data target, boundary decisions,
deviations from the target state, autonomy changes, who approved. Append rows; never edit.
Reverse a decision with a new row that references the old `Id`.

## Schema

| Column | Definition |
|---|---|
| `Id` | `D-<nnnn>` sequential |
| `Date` | ISO date |
| `Topic` | short label |
| `Decision` | what was decided (one or two lines) |
| `Approved by` | `customer (STOP x)` · `orchestrator` · `engagement brief` · `pending` |
| `Ref` | artifact / boundary id / session link |

## Log

| Id | Date | Topic | Decision | Approved by | Ref |
|---|---|---|---|---|---|
| D-0001 | 2026-09-07 | Topology | Single repo `Cognition-Partner-Workshops/ts-cobol-carddemo`, base `main`; SOURCE `app/`, BACKEND `backend/`, FRONTEND `frontend/`, DOCS `functional/CardDemo/`, REFERENCE `spring-boot/` (never the target) | engagement brief | `00_context.md` §2 |
| D-0002 | 2026-09-07 | Target state | `functional/CardDemo/CardDemo_target_state.md` (CORE + ONLINE + DATA/BOUNDARY; BATCH and SUBTRANSACTION N/A) is treated as the target state. **Customer confirmation of its PROPOSED fields (C1–C6, O1–O9, D1–D8) and flags F1–F3 is PENDING at STOP A**; `!mf_stream_migration_plan` and `!mf_program_migration` must not run before STOP A passes | pending (STOP A) | `01_target_state.md` |
| D-0003 | 2026-09-07 | Target stack | Java 21 / Spring Boot 3 / Maven / JUnit 5 / no Lombok; PostgreSQL 16 + Flyway; Angular frontend; GitHub Actions CI | engagement brief (architecture non-negotiables) | target state §3–5 |
| D-0004 | 2026-09-07 | Surfaces | BATCH and SUBTRANSACTION profiles N/A for this engagement's first stream; re-run `!mf_ingest_target_state` before any batch or called-contract stream | engagement brief | target state §6–7 |
| D-0005 | 2026-09-07 | First stream | Account View (ONLINE, `CAVW` -> `COACTVWC`, hard stop XCTL -> `COMEN01C`), owning shared programs `COSGN00C`, `COMEN01C`, `CSUTLDTC` | engagement brief | `00_context.md` §4 |
| D-0006 | 2026-09-07 | Autonomy | STOP A, B, C, E block; STOP D notifies | engagement brief | `00_context.md` §6 |
| D-0007 | 2026-09-07 | Legacy access | No mainframe/DB2; behaviour is source-derived, verified with GnuCOBOL where compilable; parity evidence is fixture-based | engagement brief | `07_runbook.md` |
| D-0008 | 2026-09-07 | Local DB | Docker `postgres:16` container `carddemo-pg` on host port **5433** (5432 is taken by a pre-installed PostgreSQL 14 that is not to be used) | setup session | `07_runbook.md` §4 |
| D-0009 | 2026-09-07 | Lead-time requests | External-team requests (data extract, SSO, …) are simulated: one file per request in `.migration/requests/`, status `SENT (simulated)` | engagement brief | `.migration/requests/README.md` |
| D-0010 | 2026-09-07 | Cross-check assets | Branches `devin/1787242078-carddemo-premigration`, `devin/batch-a-s02-account-view` (prior `.migration/`, `functional/CARDDEMO/*`, .NET `backend/`, Angular `frontend/`), branch `devin/1787158883-cobol-to-spring-boot`, `README.md`, `diagrams/`, `app/scheduler/` are cross-check only; never trusted over `app/` source, never copied | engagement brief | `00_context.md` §10 |
| D-0011 | 2026-09-07 | Encoding | Source is pure ASCII (no LATIN-1 conversion needed); ASCII data uses zoned overpunch signs -> `cobc -fsign=EBCDIC`; two copybooks contain tabs and must be re-indented before compiling | setup session (verified) | `02_conventions.md` §1 |
| D-0012 | 2026-09-07 | Artifact contract | Paths and naming in `00_context.md` §8 are binding for every later playbook | setup session | `00_context.md` §8 |
| D-0013 | 2026-09-07 | STOP A | STOP A APPROVED. CORE C1–C6, ONLINE O1–O9, DATA D1–D8 confirmed as proposed. Target state status DRAFT -> CONFIRMED | customer (STOP A) | `functional/CardDemo/CardDemo_target_state.md` §11 |
| D-0014 | 2026-09-07 | O2 / RACF boundary | BCrypt with upgrade-on-login accepted; USRSEC demo stub kept; real SSO deferred. This is the RACF/sign-on boundary decision to be registered in `04_boundary_register.md` at boundary registration | customer (STOP A) | target state §4 |
| D-0015 | 2026-09-07 | D1 / Q2 | H2 test profile only for unit `mvn verify`; Testcontainers-PostgreSQL from wave 1; CI uses a PostgreSQL 16 service container for integration tests. Parity evidence must come from real PostgreSQL 16, never H2 | customer (STOP A) | target state §5 D1, §12 Q2 |
| D-0016 | 2026-09-07 | Q1 routes | Frontend routes use business names (`/accounts/view`); the CICS trancode (`CAVW`) is recorded in FR traceability, not in the URL | customer (STOP A) | target state §12 Q1 |
| D-0017 | 2026-09-07 | Flags F1–F3 | F1: canonical DOCS folder is `functional/CardDemo/`; prior `CARDDEMO`-cased branches ignored. F2: .NET `backend/` on prior branches not reused. F3: Phase 0 creates `.github/workflows/` | customer (STOP A) | target state §1 |
| D-0018 | 2026-09-07 | Environments | Local PostgreSQL 16 via Docker on host port 5433; no mainframe, no DB2; source-derived behaviour only; working language English | customer (STOP A) | `07_runbook.md` |
| D-0019 | 2026-09-07 | STOP B | STOP B CONFIRMED. Stream S-01 AccountView = `CAVW` -> `COACTVWC`, process type **ONLINE**, hard stop = XCTL back to `COMEN01C`. Exclusions: `CAUP`/`COACTUPC` and every other menu option; the menu shell renders but only Account View is live. Touched boundaries: B-0001, B-0002, B-0006, B-0007, B-0009, B-0010, B-0014, B-0026, B-0027 | customer (STOP B) | `functional/CardDemo/CardDemo_inventory.md` §5, §8 |
| D-0020 | 2026-09-07 | CSUTLDTC ownership | `CSUTLDTC` stays in S-01 as **wave 1** although `COACTVWC` does not call it: it is a shared utility owned by this stream with no in-stream consumer yet; it gives a leaf-first wave with parity tests against COBOL-derived date expectations and de-risks the later Transaction streams (S-08 TranAdd, S-09 TranReports) which will consume the migrated version | customer (STOP B) | inventory §7, §9 risk 9; B-0014 |
| D-0021 | 2026-09-07 | B-0001, B-0002, B-0006 — VSAM KSDS leaves | Each KSDS becomes a PostgreSQL 16 table under Flyway V1 (`accounts`, `customers`, `users`) with the KSDS key as primary key, read through Spring Data JPA repositories; **no stored procedures or triggers** (D6 — the physical layer is VSAM, so the stored-procedure question does not arise: no DB2/IMS/`EXEC SQL` anywhere in the stream). NOTFND -> the legacy verbatim not-found text at 404; any other RESP -> the verbatim E-12/E-13 text at 500. Reads are idempotent, no retry. Initial load via the one-shot `import` profile (request R-0001); VSAM stays source of truth until STOP E, refreshed by re-import, no dual-write | STOP C approved 2026-09-07 (D-0033) | `functional/CardDemo/AccountView_migration_plan.md` §3.1, §4 |
| D-0022 | 2026-09-07 | B-0007 — CXACAIX AIX and multi-card semantics (Q-04) | The `CCXREF` base cluster becomes table `card_xrefs`; the `CXACAIX` alternate index becomes a B-tree index `idx_card_xrefs_acct_id` (D-0013 D3). The legacy "first record on the AIX path wins" is replaced by an explicit deterministic contract — **lowest card number wins** (`findFirstByXrefAcctIdOrderByXrefCardNumberAsc`). Parity implication: the legacy order is undefined in the repo; on the committed fixture (50 xrefs over 50 distinct accounts, measured) both behave identically, so the deviation is unobservable today and becomes observable only if a multi-card account appears. R-0001 asks for the `DEFINE AIX` uniqueness | STOP C approved 2026-09-07 (D-0033) | plan §3.1, §10 Q-04; analysis R-03 |
| D-0023 | 2026-09-07 | B-0033 — menu option table | `COMEN02Y` becomes a static route table in code (`MenuOption` records in `service/MenuService`), not a database table: it is program constant data that changes only when a stream is migrated. Each later stream flips its own `implemented` flag — the single switch that opens an option | STOP C approved 2026-09-07 (D-0033) | plan §3.1 |
| D-0024 | 2026-09-07 | B-0009 and Q-01 — sign-on routing and admin users | Sign-on returns `{userId,userType,landingTarget}` and the SPA routes to `/menu`; the XCTL disappears. An admin (`SEC-USR-TYPE='A'`) **authenticates normally and lands on the same `/menu`** with the banner `Administration is not available in this release`; no admin route exists in S-01 and admin sign-on is not refused (the legacy does not refuse it). `landingTarget` is the routing point that flips to the admin menu when S-11 migrates | STOP C approved 2026-09-07 (D-0033) | plan §3.2, §10 Q-01; analysis R-11 |
| D-0025 | 2026-09-07 | B-0010, B-0032 and Q-12 — menu dispatch, excluded options, hard stop | Dispatch becomes `POST /api/menu/select` -> `{program,endpoint,implemented,message}`. Option 1 is live and routes to `/accounts/view`. **Options 2..11 (including option 11/COPAUS0C, B-0032) render with their verbatim legacy labels, visually disabled, and return `implemented:false` with the target-only text `Option not available in this release`** (marked NEW TEXT, not a legacy literal); no deep link to the legacy CICS screens, because the two systems share no session. The hard stop (F3 on Account View) routes to `/menu` discarding search state; menu Exit routes to `/signon` and invalidates the session. **This route table is the strangler-fig facade for the whole module** and retires option by option | STOP C approved 2026-09-07 (D-0033) | plan §3.2, §10 Q-12; FR-10..FR-12, FR-21 |
| D-0026 | 2026-09-07 | B-0028 — pseudo-conversation | `RETURN TRANSID` + COMMAREA round-trips are dropped; each HTTP request is self-contained. The `CDEMO-PGM-CONTEXT` 0/1 enter/re-enter distinction becomes the difference between the initial screen render (FR-13) and a submitted search request (FR-14..FR-19) | STOP C approved 2026-09-07 (D-0033) | plan §3.2 |
| D-0027 | 2026-09-07 | B-0014 — CSUTLDTC port | Ported once in wave 1 as an in-process Spring `@Service` with a typed record contract `DateValidationResult(severity, messageNumber, reasonText, formatted80)`; the 80-byte `CSUTLDWY`-shaped rendering is retained so a legacy-shaped caller can still compare strings, while callers are expected to branch on severity/message number. An invalid date is a result, never an exception. Owned by S-01; S-08, S-09 and S-02 inherit the port; the legacy load module retires when its last caller migrates, not at S-01 sign-off | STOP C approved 2026-09-07 (D-0033) | plan §3.3; D-0020 |
| D-0028 | 2026-09-07 | B-0015 and Q-08 — CEEDAYS (deferral with re-entry condition) | Substituted by `java.time` with `ResolverStyle.STRICT`. **Contractual**: `0000`/`Date is valid` and `2513`/`Unsupp. Range` — the only tokens callers branch on. **INFERRED**: 2507, 2508, 2509, 2517, 2518, 2520, 2521, each mapped from a distinguishable parse failure and labelled INFERRED in the wave-1 program FR; anything unmapped renders `Date is invalid` as the COBOL does. **Dependency**: a real Language Environment run or IBM documentation of the CEEDAYS feedback catalogue. **Impact**: no S-01 requirement is affected (no in-stream caller); S-08/S-09 parity for the non-contractual reason texts is unproven. **Re-entry condition**: re-open B-0015 when S-08 TranAdd or S-09 TranReports is planned, or earlier if a catalogue becomes available | STOP C approved 2026-09-07 (D-0033) | plan §3.3, §10 Q-08; analysis R-10 |
| D-0029 | 2026-09-07 | B-0026 and Q-13 — sign-on / RACF substitute (deferral of real SSO) | Keep the USRSEC demo stub; store BCrypt hashes; accept a legacy plaintext value once per user and rewrite it as a hash on the next successful sign-on; upper-case the input before hashing and comparison so the legacy case-insensitivity (FR-04) is preserved exactly; the legacy plaintext column is cleared on upgrade and never returned by any endpoint. No lockout and no audit trail — deliberate parity with the legacy. **Deferral of real SSO** (D-0014): dependency = IdP client registration and a user-id mapping owner (request R-0002); impact = the target owns credentials with no MFA, rotation, lockout or real audit, carrying the legacy exposure forward; re-entry condition = R-0002 answered, or S-11 UserAdmin planned, or any non-demo environment holding real user data, whichever comes first | STOP C approved 2026-09-07 (D-0033) | plan §3.3, §10 Q-13; `.migration/requests/R-0002_sso_idp_enablement_signon.md` |
| D-0030 | 2026-09-07 | B-0027 and Q-05 — COMMAREA -> HTTP session | The COMMAREA is replaced by a server-side HTTP session holding `userId` and `userType` only; screen-to-screen context travels in request/response DTOs and Angular component state. The legacy user-id wipe on entry to `COACTVWC` and the unconditional `CDEMO-USER-TYPE <- 'U'` on PF3 are **not reproduced** (deviation DV-02): the wipe depends on residual storage (INFERRED) and is not a business rule. Tests assert that identity survives menu -> account view -> menu | STOP C approved 2026-09-07 (D-0033) | plan §3.3, §9 DV-02, §10 Q-05; analysis R-08 |
| D-0031 | 2026-09-07 | B-0029, B-0030 and Q-06, Q-11, Q-14 — screen I/O and system services | BMS maps become Angular 18 + Material components, one per map, with labels taken verbatim from the BMS source; money keeps the `PICOUT '+ZZZ,ZZZ,ZZZ.99'` rendering and SSN the `nnn-nn-nnnn` rendering. PF keys are not represented in the API: buttons `Sign on`/`Exit`, `Continue`/`Exit`, `Search`/`Exit` with `F3`/`Esc` bound to Exit; FR-25 (invalid key) is dropped as it has no browser trigger (DV-03), and "any key acts as ENTER" is not reproduced. ZIP and phone show the **full** stored values instead of the map's 5/13-character truncation (DV-05). Explicit request-payload validation replaces the never-tested `RECEIVE MAP RESP` (Q-14). `APPLID`/`SYSID` become configuration properties and date/time come from an injectable server clock | STOP C approved 2026-09-07 (D-0033) | plan §3.3, §9, §10 Q-06/Q-11/Q-14; analysis R-09, R-12, R-13 |
| D-0032 | 2026-09-07 | B-0031, Q-03, Q-09, Q-10 — failure handling, the account-missing quirk and inferred formats | (a) The CICS abend protocol becomes a generic HTTP 500 from `GlobalExceptionHandler` with no stack trace; the stream reads only, so nothing is backed out. (b) **Q-03: the account-missing quirk is treated as a defect and fixed** — after an ACCTDAT not-found the target returns E-10 only, with no account or customer payload (deviation DV-01); the legacy behaviour displays an unread `ACCOUNT-RECORD` area and would ship undefined data. (c) **Q-09**: the literal parts of E-09..E-12 are reproduced verbatim and the RESP/RESP2 tail is rendered as a reason code; byte parity with the compiler's numeric move rules is explicitly not a goal and cannot be verified off-host. (d) **Q-10**: account/customer `X(10)` dates map to `DATE`/`LocalDate` subject to a wave-1 gate — the importer validates every date value in the full extract and a single non-ISO value flips the column to `VARCHAR(10)` with raw text in the DTO, matching the legacy unvalidated move; the outcome is recorded as its own decision row in wave 1 | STOP C approved 2026-09-07 (D-0033) | plan §3.3, §4.2, §9, §10 Q-03/Q-09/Q-10; analysis R-02, R-04, R-05, R-06 |
| D-0033 | 2026-09-07 | STOP C | STOP C APPROVED. Data target PostgreSQL 16 + Flyway; VSAM stays source of truth until sign-off; one-way re-import (D8). Boundary decisions D-0021..D-0032 approved as decided for all 16 touched boundaries (B-0026 demo USRSEC stub, SSO deferred, R-0002 SENT simulated; B-0015 CEEDAYS deferred with its stated re-entry condition). 4 sequential waves approved (W1 data seams + CSUTLDTC, W2 sign-on, W3 menu shell, W4 Account View). Phase 0 deltas (`backend/`, `frontend/`, Flyway V1, `.github/workflows/`) approved. PR shape: one PR per wave into the stream branch, single merge to main at STOP E | customer, STOP C (parent session) | `functional/CardDemo/AccountView_migration_plan.md` |
| D-0034 | 2026-09-07 | STOP C — deviations | DV-01..DV-05 approved. Each must be asserted explicitly in parity tests (no silent drift) and listed in the owning program FR as a deliberate deviation | customer, STOP C (parent session) | plan §9 |
| D-0035 | 2026-09-07 | STOP C — open questions | Q-01..Q-14 PROPOSED resolutions accepted as written. Q-04: keep "lowest card number wins"; the R-0001 AIX-uniqueness question is carried forward as an open risk into sign-off. Q-10: the wave-1 importer date-gate result must be written to this log before wave 2 starts | customer, STOP C (parent session) | plan §10 |
| D-0036 | 2026-09-07 | STOP C — autonomy | STOP D notifies only: post each wave's PR + parity verdict and continue unless parity fails; on failure return to `!mf_program_migration`, re-run parity, report what failed and how it was fixed. A downstream wave must not start before the upstream wave is green and merged into the stream branch | customer, STOP C (parent session) | `00_context.md` |
| D-0037 | 2026-09-07 | Wave 1 — Q-10 importer date gate | Measured by the wave-1 importer over the full `app/data` extract: 200 date values (50 accounts x ACCT-OPEN/EXPIRAION/REISSUE + 50 customers x CUST-DOB), 0 offenders, all `YYYY-MM-DD`. Decision: keep `DATE`/`LocalDate` for the four date columns; the `VARCHAR(10)` fallback is not taken. Pending independent re-measure by the wave-1 parity child (PASS confirms; a refutation reopens this row) | orchestrator, per D-0035 gate rule | PR #95, `AccountView_migration_plan.md` §4.2 |
| D-0038 | 2026-09-07 | Wave 1 — ACCTDAT ZIP/GROUP-ID slot quirk (PENDING STOP D reply, default proposed) | Parity found every `acctdata.txt` record has `A000000000` in the ACCT-ADDR-ZIP slot (bytes 103-112) and blanks in ACCT-GROUP-ID (bytes 113-122). The importer, per the STOP-A-confirmed data quirk (`CardDemo_target_state.md:146`, `carddemo.import.acctdata-group-id-in-zip-slot=true`), stores `acct_group_id='A000000000'`, `acct_addr_zip=NULL`. Consequence: legacy `COACTVWC.cbl:485` displays a blank Account Group; the target would display `A000000000`. PROPOSED: treat as deliberate deviation **DV-06** (target shows the group id the extract evidently intends; a DISCGRP key), asserted explicitly in wave-4 parity, and add the extract column question to R-0001. Default applies at wave 4 unless the STOP D reply says "reproduce blank group" | orchestrator (proposal); approval pending | `parity/wave1/Wave1_parity_results.md` §4.2 |
| D-0039 | 2026-09-07 | Wave 1 close | PR #95 CI green (backend verify, frontend build-test), independent parity PASS (2 PASS-WITH-RISK units, 0 FAIL), merged into the stream branch as `5f1f708`; parity evidence merged as `0f9470a`. D-0037 (Q-10) confirmed by independent re-measure (200/200). Wave 2 authorized per D-0036 | orchestrator, per D-0036 | PR #95; child sessions 1199271159c447b9ab380e25b0aac25c, f663ab978f5740349b2f99ac17444780 |
| D-0040 | 2026-09-07 | Wave 2 close | PR #96 first parity run: 2 FAIL (E-13 `WHEN OTHER` returned generic text when PostgreSQL unreachable, `COSGN00C.cbl:252-256`; E-06 focus went to User ID instead of Password, `:244`) + 2 defects (wave-1 `PostgresIntegrationTest` static container stopped after first subclass; headless import command broken by `@ConditionalOnWebApplication` encoder). Returned to `!mf_program_migration`; fixed in `60413a0` (DataAccessException/TransactionException -> E-13; focus fix + Karma test; singleton never-stopped container; `PasswordEncoderConfig` non-web). Parity re-run 20/20 PASS (1 PASS-WITH-RISK = approved Q-01). CI green; merged into stream branch as `f65d45b`, parity evidence `f559b8a`. Note: `AuthService.signOn` no longer `@Transactional` (lookup and upgrade-save in separate transactions; behaviour unchanged). Wave 3 authorized per D-0036 | orchestrator, per D-0036 | PR #96; `parity/wave2/Wave2_parity_results.md` §11 |
| D-0041 | 2026-09-07 | Wave 3 close + DV-07 | PR #97 (COMEN01C) CI green; parity 19/19 PASS (1 PASS-WITH-RISK = Q-14 explicit payload validation), 0 FAIL, first pass. Merged into stream branch `312f2f2`, parity evidence `e3b76bc`. Parity T-3 register gap: legacy evaluates `CDEMO-MENU-OPT-USRTYPE(WS-OPTION)` for options 12..99 after already sending E-08 (`COMEN01C.cbl:127-143`, subscript past the 11-row table `COMEN02Y.cpy:94`): storage-dependent garbage read / SSRANGE abend / possible second SEND `No access - Admin Only option...`. Target returns E-08 only. Recorded as **DV-07** (already described in FR §5 V-3 / §7 `option 12..99`, approved at STOP C as part of the FR; now listed explicitly so parity asserts it). No observable difference demonstrable off-host. Wave 4 authorized per D-0036 | orchestrator, per D-0036 (DV-07 = STOP C-approved FR content made explicit) | PR #97; `parity/wave3/COMEN01C_parity_results.md` §5 |
| D-0042 | 2026-09-07 | Wave 4 close | PR #98 (COACTVWC) first parity: backend PASS (API field parity 145/145 on 5 fixture accounts vs raw ACCTDAT/CUSTDAT bytes, real PG 16), UI 3 FAIL: F-1 header (title/date/time) empty until first search (`COACTVWC.cbl:431-453`); F-2 blank input did not set ACCTSID to red `*` (`:555-565`); F-3 FICO `78` vs legacy `078` (`:505-506`). Returned to `!mf_program_migration`; fixed in `f16c141` (header endpoint on init; `*` + error attribute + cursor; `ficoScoreDisplay`; E-11/E-12 tests assert 75-byte `WS-RETURN-MSG` ceiling). Re-run 24/24: 20 PASS, 3 PASS-WITH-RISK (DV-06 approved; F5 = browser reload; container-level 400s), 0 FAIL, 1 UNTESTED (generic 500 path, no injection point). Merged into stream branch `7177926`, parity `0de7321`. Parity rulings adopted: (a) legacy error texts E-09/E-10 lose 6 RESP2 chars, E-11 loses 3, E-12 fits — the 75-byte text is the observable truth; FR §5/§7 literals need a footnote (sign-off item); (b) FR §3.2 `ficoScore Integer` drops the leading zero — display contract is X(3) (FR footnote, sign-off item); (c) DV-03 wording: browser-native keys (F5 reload) are out of scope (register wording, sign-off item). DV-06 (D-0038) applied by default — no override received. Breaker count: UI-render class 1, message class 1 (<3). Stream implementation complete; proceed to UI testing + sign-off | orchestrator, per D-0036 | PR #98; `parity/wave4/COACTVWC_parity_results.md` §10 |
